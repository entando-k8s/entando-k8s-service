# Database
spring.datasource.url=jdbc:${DB_VENDOR:postgresql}://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:digital_exchange_test}?${DB_OPTIONS:useSSL=false}
spring.datasource.username=${DB_USER:admin}
spring.datasource.password=${DB_PASS:admin}

#Security
spring.security.oauth2.client.provider.oidc.issuer-uri=http://DONOTCALL/auth/realms/entando
spring.security.oauth2.client.registration.oidc.client-id=my-digital-exchange
spring.security.oauth2.client.registration.oidc.client-secret=e52c1e9a-065b-49fc-9ce8-fa6913bd87d0

# Entando
entando.namespaces.to.observe=test-namespace, my-namespace
